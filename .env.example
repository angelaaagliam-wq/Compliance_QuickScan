# Where Make receives the completed files (required)
OUTPUT_WEBHOOK_URL=https://hook.make.com/your-url

# Score band thresholds (optional — defaults below)
BAND_HIGH_MAX=35
BAND_MID_MAX=69

# Optional: secure your incoming endpoint
# Add header x-webhook-secret: <value> in Make's HTTP module
WEBHOOK_SECRET=
